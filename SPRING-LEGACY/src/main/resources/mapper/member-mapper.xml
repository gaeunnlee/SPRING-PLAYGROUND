<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.crud_ex.mapper.MemberMapper">

    <select id="findAll" resultType="org.crud_ex.domain.MemberVO">
        SELECT * FROM member
    </select>

    <select id="findCreatedAtById" parameterType="string" resultType="java.time.LocalDateTime">
        SELECT created_at
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <select id="findByProviderAndUserId"
            resultType="org.crud_ex.domain.MemberIdentityVO">
        SELECT
            member_identity_id,
            member_id,
            provider,
            provider_user_id,
            created_at
        FROM member_identity
        WHERE provider = #{provider}
          AND provider_user_id = #{providerUserId}
            LIMIT 1
    </select>

    <select id="findByMemberId" parameterType="string" resultType="org.crud_ex.domain.MemberVO">
        SELECT
            member_id AS memberId,
            email,
            password_hash AS passwordHash,
            name,
            age,
            status
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <select id="findByEmail" parameterType="string" resultType="org.crud_ex.domain.MemberVO">
        SELECT
            member_id AS memberId,
            email,
            password_hash AS passwordHash,
            name,
            age,
            status
        FROM member
        WHERE email = #{email}
    </select>

    <insert id="insert" parameterType="org.crud_ex.domain.MemberVO">
        INSERT INTO member(member_id, email, password_hash, name, age, status)
        VALUES(#{memberId}, #{email}, #{passwordHash}, #{name}, #{age}, 'ACTIVE')
    </insert>

    <update id="update" parameterType="org.crud_ex.domain.MemberVO">
        UPDATE member
        SET name = #{name},
            age  = #{age}
        WHERE member_id = #{memberId}
    </update>

    <delete id="delete" parameterType="string">
        DELETE FROM member WHERE member_id = #{memberId}
    </delete>

</mapper>
